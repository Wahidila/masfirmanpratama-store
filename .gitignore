.env
.env.local
node_modules/
.next/
dist/
build/
*.log
.DS_Store

# MC infra (per-environment, bukan project code)
.discord.json
.kanban-task-ids.json

# Third-party reference plugin (GPL WooCommerce, audit-only — jangan masuk repo)
docs_dev/reference/agenwebsite-shipping/

# TailAdmin Laravel template (UI reference only, verbatim port source — jangan masuk repo)
_ui_reference/
